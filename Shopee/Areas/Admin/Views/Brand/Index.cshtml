@model IEnumerable<Shopee.Models.lkpBrand>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
@*
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BrandName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BrandName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Active)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BrandID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BrandID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BrandID })
        </td>
    </tr>
}

</table>
*@
<section class="panel">
                          <header class="panel-heading">
                             List All Brands<span class="tools pull-right">
                                        <i class="glyphicon glyphicon-plus"></i>
                                        @Html.ActionLink("New", "Create")
                                     </span>
                          </header>
                          <table class="table table-striped table-advance table-hover">
                              <thead>
                              <tr>
                                  <th><i class="fa fa-bullhorn"></i> Brand Name</th>                                  
                                  <th><i class=" fa fa-edit"></i> Status</th>
                                  <th></th>
                              </tr>
                              </thead>
                              <tbody>
                              @foreach (var item in Model)
                              {
                              <tr>
                                  <td>   @Html.ActionLink(item.BrandName, "Edit", new { id = item.BrandID }) </td>
                                
                                 
                                  <td><span class="label 
                                  @if (item.Active.Value) { Html.Raw("label-success"); }
                                  else
                                  {
                                      Html.Raw("label-error");
                                  }
                                                             label-mini"> </span></td>
                                  <td>
                                  
                                   <button class="btn btn-danger btn-xs pull-right" data-placement="left"  onclick="javascript:return ShowConfirmDelete('@Url.Action("delete", "Brand", new { @id = item.BrandID })');" data-toggle="tooltip" title="Delete" ><i class="glyphicon glyphicon-trash "></i></button>
                                     
                                  </td>
                              </tr>
                              }
                              </tbody>
                          </table>
                      </section> 